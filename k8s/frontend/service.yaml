apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: crazy-cookies
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: crazy-cookies
spec:
  selector:
    app: frontend
  ports:
    - port: 3001
      targetPort: 80
      nodePort: 30080
      name: http
  type: NodePort

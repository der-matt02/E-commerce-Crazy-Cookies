apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: crazy-cookies
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: crazy-cookies
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30090
      name: http
  type: NodePort
